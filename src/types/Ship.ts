export type Ship = {
    id: string;
    name: string;
    roles: string[];
    active: boolean;
    image: string;
    legacy_id: string;
    type: string;
    home_port: string;
    link: string;
    launches: string[];
};
